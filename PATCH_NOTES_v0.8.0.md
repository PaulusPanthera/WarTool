# WARtool v0.8.0 patch notes

- connected both published team catch tabs
- connected the published Settings tab
- added scheduled and manual GitHub Action imports
- removed all need for browser-side Google Sheet fetching
- added strict source/header validation and row-level warnings
- added same-origin `state.json` generation
- added public `import-report.json`
- added local `UPDATE_LIVE_DATA.bat` for testing
- preserved the existing v0.7 browser settings/storage key
- kept the committed release state empty until the GitHub Action runs
